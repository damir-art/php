# explode()
Создаёт массив из строки, элементы создаются по разделителю.

    $words = "Иванов Иван Иванович";
    $res = explode(" ", $words);

    echo "<pre>";
    print_r($res);
    echo "</pre>";

    Array (
        [0] => Иванов
        [1] => Иван
        [2] => Иванович
    )
