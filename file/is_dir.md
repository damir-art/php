# is_dir()
Проверяет является ли папкой:

    if(is_dir("folder")) {
        echo "Папка";
    } else {
        echo "Не папка / отсутствует";
    }

## Дополнительно
- `.` - это каталог
- `..` - это каталог
